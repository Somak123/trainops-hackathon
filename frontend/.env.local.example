# Backend (Dev 2's FastAPI control plane)
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# DigitalOcean Spaces (S3-compatible) — POC ONLY: hardcoded in the browser bundle.
# DO NOT ship to production with these prefixed NEXT_PUBLIC_; rotate the keys
# before/after the demo. For internal POC testing this is fine.
NEXT_PUBLIC_DO_SPACES_KEY=
NEXT_PUBLIC_DO_SPACES_SECRET=
NEXT_PUBLIC_DO_SPACES_REGION=nyc3
NEXT_PUBLIC_DO_SPACES_BUCKET=trainops-datasets
